spring:
  application:
    name: apigateway
server:
  port: 9000
  servlet:
    context-path: /api
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true

